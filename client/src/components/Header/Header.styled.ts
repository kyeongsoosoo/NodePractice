import styled from "styled-components";
import { mainColor } from "../../lib/css/Theme";

const HeaderWrapper = styled.div`
  background-color: ${mainColor.lime3};
  width: 100%;
  height: 15%;
`;

export default {
  HeaderWrapper,
};
